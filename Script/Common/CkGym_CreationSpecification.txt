// Language-angelscript

//============================================================================
// CK FRAMEWORK GYM CREATION SPECIFICATION
//============================================================================

/*
CRITICAL: This specification describes the gym FRAMEWORK — the project-agnostic
base classes, station spawning, auto-mode infrastructure, and cycler/HUD that
live in CkTests/Script/Common/. The framework itself has zero knowledge of
any specific gym.

CONSUMERS register their gyms at runtime from a project-level base game mode:
  - CkTests gyms: ACkTests_Gym_Base_GameMode → CkTests_Gyms::RegisterAll()
  - Your project:  AMyProject_Gym_Base_GameMode → MyProject_Gyms::RegisterAll()
Each level is self-contained — the Tab menu shows only gyms registered by that
level's game mode. See "PROJECT-LEVEL GAME MODES" below.
*/

//============================================================================
// 1. ARCHITECTURE OVERVIEW
//============================================================================

// Client-Server Safe Architecture:
// - All gym logic is in PlayerController (exists on both client/server)
// - No dependencies on GameMode availability timing
// - GameMode is minimal - just inheritance setup
// - PlayerController contains all metadata and startup logic

//============================================================================
// 2. BASE CLASSES (REQUIRED)
//============================================================================

// FRAMEWORK base classes (do not inherit directly — use project base):
//   ACk_Gym_Base_GameMode          — sets HUD, default pawn, station spawning
//   ACk_Gym_Base_PlayerController  — station lookup, grid layout, gym cycling
//   ACk_Gym_Base_Pawn              — ECS-ready DefaultPawn (use directly or subclass)
//
// PROJECT-LEVEL base game modes (inherit your gym GameMode from one of these):
//   CkTests:      ACkTests_Gym_Base_GameMode  (Script/Common/CkTests_Gym_Base_GameMode.as)
//   Your project: Create your own following the same pattern — a subclass of
//                 ACk_Gym_Base_GameMode that calls your registration namespace
//                 in BeginPlay. See CkTests_Gym_Base_GameMode.as as the template.
//
// These call their project's registration namespace in BeginPlay, which feeds
// the Tab menu via CkGym_Cycler::RegisterProjectGym().

//============================================================================
// 3. FILE STRUCTURE STANDARD
//============================================================================

// Required file structure for each gym:
// Script/CkGyms/[GymName]/
// ├── Ck[GymName]Gym_GameMode.as
// ├── Ck[GymName]Gym_PlayerController.as
// ├── Ck[GymName]Gym_Pawn.as (optional - can use base class directly)
// └── Ck[GymName]Gym_[FeatureScripts].as (feature-specific entity scripts, assets, etc.)

//============================================================================
// 4. NAMING CONVENTIONS
//============================================================================

// Class Names:
// GameMode: ACk_[GymName]Gym_GameMode
// PlayerController: ACk_[GymName]Gym_PlayerController
// Pawn: ACk_[GymName]Gym_Pawn (if needed)
// Entity Scripts: UCk_[GymName][Feature]_EntityScript

// FName Tags for Station Discovery (hierarchical):
// Format: Gym.[GymCategory].[SpecificFeature]
// Examples:
// - Gym.Audio.BackgroundMusic
// - Gym.Audio.SpatialAudio
// - Gym.Physics.RigidBody
// - Gym.Rendering.MaterialSwap

// Console Commands:
// Format: Ck_Gym[GymName]_[Action]
// Examples:
// - Ck_GymAudioSimple_RestartBackgroundMusic
// - Ck_GymPhysics_ResetSimulation

//============================================================================
// 5. GAMEMODE TEMPLATE (MINIMAL)
//============================================================================

// GameMode is minimal — all logic lives in PlayerController.
// Inherit from your PROJECT's base game mode, not ACk_Gym_Base_GameMode directly.
//
// CkTests example:
class ACk_[GymName]Gym_GameMode : ACkTests_Gym_Base_GameMode
{
    default PlayerControllerClass = ACk_[GymName]Gym_PlayerController;
    default DefaultPawnClass = ACk_Gym_Base_Pawn;
}
//
// Project gym example (your own base game mode):
// class AMyProject_[GymName]Gym_GameMode : AMyProject_Gym_Base_GameMode
// {
//     default PlayerControllerClass = AMyProject_[GymName]Gym_PlayerController;
//     default DefaultPawnClass = ACk_Gym_Base_Pawn;
// }
//
// REGISTRATION: After creating the GameMode, add it to your project's registry
// namespace so it appears in the Tab menu:
//
// CkTests:      CkTests_GymRegistry.as     → CkGym_Cycler::RegisterProjectGym("Name", ACk_XxxGym_GameMode);
// Your project: MyProject_GymRegistry.as   → CkGym_Cycler::RegisterProjectGym("Name", AMyGym_GameMode, "MyGymLevel");
//
// The LevelName parameter is optional — omit it to use the subsystem's default
// (TestGyms_CkTests_Level). Project gyms that live in their own levels should provide
// their level name explicitly.

//============================================================================
// 6. PLAYERCONTROLLER TEMPLATE (MAIN GYM LOGIC)
//============================================================================

class ACk_[GymName]Gym_PlayerController : ACk_Gym_Base_PlayerController
{
    //------------------------------------------------------------------------
    // STATION DEFINITION (REQUIRED)
    //------------------------------------------------------------------------

    TArray<FCkGym_Station_SpawnParams_Payload> Get_RequiredStations() override
    {
        auto Stations = TArray<FCkGym_Station_SpawnParams_Payload>();

        // Define each station with tag, title, and description
        {
            auto Station = FCkGym_Station_SpawnParams_Payload();
            Station.Tags.Add(n"Gym.[Category].[Feature1]");
            Station.Title = FText::FromString("FEATURE 1 TITLE");
            auto Description = TArray<FText>();
            Description.Add(FText::FromString("Description paragraph 1."));
            Description.Add(FText::FromString("Description paragraph 2."));
            Station.Description = Description;
            Stations.Add(Station);
        }

        // Add more stations as needed
        {
            auto Station = FCkGym_Station_SpawnParams_Payload();
            Station.Tags.Add(n"Gym.[Category].[Feature2]");
            Station.Title = FText::FromString("FEATURE 2 TITLE");
            auto Description = TArray<FText>();
            Description.Add(FText::FromString("What this station demonstrates."));
            Station.Description = Description;
            Stations.Add(Station);
        }

        return Stations;
    }

    //------------------------------------------------------------------------
    // MAIN GYM STARTUP LOGIC
    //------------------------------------------------------------------------

    void Request_StartGym() override
    {
        // Base class spawns stations before calling this
        // Implement gym-specific startup logic here
        Request_Start[Feature1]();
        Request_Start[Feature2]();

        ck::Trace("[GymName] Gym - All features started");
    }

    //------------------------------------------------------------------------
    // FEATURE IMPLEMENTATION FUNCTIONS
    //------------------------------------------------------------------------

    void Request_Start[Feature1]()
    {
        // Get station transform using base class utility (handles missing stations gracefully)
        auto StationTransform = Get_StationTransform("Gym.[Category].[Feature1]");

        // Implement feature startup logic:
        // - Spawn entities at station location
        // - Execute audio cues
        // - Start physics simulations
        // - etc.

        ck::Trace("✅ [Feature1] started at station location");
    }

    void Request_Start[Feature2]()
    {
        auto StationTransform = Get_StationTransform("Gym.[Category].[Feature2]");

        // Implement second feature startup logic

        ck::Trace("✅ [Feature2] started at station location");
    }

    //------------------------------------------------------------------------
    // CONSOLE COMMANDS FOR TESTING
    //------------------------------------------------------------------------

    UFUNCTION(Exec, DisplayName="[GymName] Gym - Restart [Feature1]")
    void Ck_Gym[GymName]_Restart[Feature1]()
    {
        Request_Start[Feature1]();
    }

    UFUNCTION(Exec, DisplayName="[GymName] Gym - Restart [Feature2]")
    void Ck_Gym[GymName]_Restart[Feature2]()
    {
        Request_Start[Feature2]();
    }

    // Add more console commands as needed for individual feature testing
}

//============================================================================
// 7. PAWN TEMPLATE (OPTIONAL)
//============================================================================

// Only create custom pawn if special behavior is needed
// Otherwise, use ACk_Gym_Base_Pawn directly in GameMode
UCLASS()
class ACk_[GymName]Gym_Pawn : ACk_Gym_Base_Pawn
{
    // Override Request_OnPawnReady() if custom pawn behavior is needed
    void Request_OnPawnReady() override
    {
        // Custom pawn initialization logic here
        Super::Request_OnPawnReady();
    }
}

//============================================================================
// 8. STATION SETUP (AUTOMATIC)
//============================================================================

// Stations are now spawned automatically via Get_RequiredStations() override
// Base class handles:
// - Default grid layout (500 units forward, spaced 800 units along Y axis)
// - Stations face player (180° rotation)
// - Centered grid arrangement
// - Skip spawning if station already exists in level

// Station Spawn Parameters:
// - Tags: FName array for station identification
// - Title: FText displayed at top of station
// - Description: TArray<FText> for multi-paragraph descriptions
// - Transform: Optional override (defaults to grid layout)
// - Height: Use gym_auto::EstimateStationHeight() to compute from content
// - Dimensions, colors, text scaling (use defaults unless needed)
//
// STATION HEIGHT — ESTIMATION & NORMALIZATION:
// Use gym_auto::EstimateStationHeight() to compute station height from the
// number of auto steps, manual commands, and expected live-data lines.
// This avoids hardcoding heights that drift out of sync with content.
//
//   Station.Height = gym_auto::EstimateStationHeight(
//       6,    // auto step count
//       1,    // manual command count
//       14);  // expected live-data lines
//
// After building all stations, call gym_auto::NormalizeStationHeights() to
// set every station to the tallest height, so all displays in a gym are
// the same size:
//
//   gym_auto::NormalizeStationHeights(Stations);
//   return Stations;

// Manual Override:
// - Place BP_DemoDisplay in level with matching tag to override automatic spawn
// - Useful for custom positioning or special station configurations

// Tag Naming Rules:
// 1. Always start with "Gym."
// 2. Second level: broad category (Audio, Physics, Rendering, etc.)
// 3. Third level: specific feature being tested
// 4. Use PascalCase for readability
// 5. Keep names descriptive but concise

//============================================================================
// 9. BUILT-IN CONSOLE COMMANDS (INHERITED)
//============================================================================

// Standard commands available in ALL gyms (inherited from base class):
// Ck_Gym_ShowInfo - Display gym name, description, and required stations
// Ck_Gym_ValidateStations - Validate all required stations exist in level
// Ck_Gym_Restart - Restart the entire gym

//============================================================================
// 10. LOGGING STANDARDS
//============================================================================

// Use standardized logging functions:
// ck::Trace() - General information, successful operations
// ck::Warning() - Non-critical issues, fallbacks used
// ck::Error() - Critical failures, missing requirements

// Logging Patterns:
// Successful operations:
ck::Trace("✅ [Feature] started successfully");
ck::Trace("🎵 Background music playing"); // Use emojis for visual categorization

// Warnings:
ck::Warning("❌ Station not found, using default location");

// Errors:
ck::Error("❌ Critical component missing - [Feature] cannot start");

//============================================================================
// 11. COMMON IMPLEMENTATION PATTERNS
//============================================================================

//------------------------------------------------------------------------
// AUDIO GYM PATTERN
//------------------------------------------------------------------------

void Request_StartAudioFeature()
{
    auto StationTransform = Get_StationTransform("Gym.Audio.FeatureName");

    utils_cue_executor::Request_ExecuteCue_Local(ck::ToEntity(this),
        utils_gameplay_tag::ResolveGameplayTag(n"Audio.Cue.Tag"),
        FAudioCueTransform(StationTransform));

    ck::Trace("🎵 Audio feature started at station location");
}

//------------------------------------------------------------------------
// ENTITY SPAWNING PATTERN
//------------------------------------------------------------------------

void Request_SpawnEntityAtStation()
{
    auto StationTransform = Get_StationTransform("Gym.Category.Feature");
    auto SpawnParams = FCk_EntityScript_SpawnParams_WithTransform();
    SpawnParams.Set_Transform(StationTransform);

    auto SpawnRequest = utils_entity_script::Request_SpawnEntity(
        ck::ToEntity(this),
        UCk_MyEntityScript,
        SpawnParams
    );

    if (!ck::IsValid(SpawnRequest))
    {
        ck::Error("❌ Failed to spawn entity at station");
        return;
    }

    ck::Trace("✅ Entity spawned at station location");
}

//------------------------------------------------------------------------
// PHYSICS GYM PATTERN
//------------------------------------------------------------------------

void Request_StartPhysicsFeature()
{
    auto StationTransform = Get_StationTransform("Gym.Physics.RigidBody");

    // Spawn physics entities at station
    // Set up physics simulation parameters
    // Add timer systems for automated testing

    ck::Trace("⚡ Physics feature started at station location");
}

//============================================================================
// 12. ERROR HANDLING PATTERNS
//============================================================================

//------------------------------------------------------------------------
// STATION ACCESS (AUTOMATIC VIA BASE CLASS)
//------------------------------------------------------------------------

// Base class automatically validates all required stations before starting gym
// Individual station access with graceful fallbacks:
auto StationTransform = Get_StationTransform("Gym.Category.Feature");
// Base class handles missing station warnings and returns FTransform::Identity as fallback

//------------------------------------------------------------------------
// ENTITY SPAWNING ERROR HANDLING
//------------------------------------------------------------------------

auto SpawnRequest = utils_entity_script::Request_SpawnEntity(/* params */);
if (!ck::IsValid(SpawnRequest))
{
    ck::Error("❌ Failed to spawn [Entity] - check entity script setup");
    return;
}
ck::Trace("✅ [Entity] spawned successfully");

//------------------------------------------------------------------------
// AUDIO CUE ERROR HANDLING
//------------------------------------------------------------------------

auto AudioCueResult = utils_cue_executor::Request_ExecuteCue_Local(/* params */);
if (!ck::IsValid(AudioCueResult))
{
    ck::Error("❌ Failed to execute audio cue - check cue setup");
    return;
}
ck::Trace("🎵 Audio cue executed successfully");

//============================================================================
// 13. DEVELOPMENT WORKFLOW CHECKLIST
//============================================================================

/*
BEFORE IMPLEMENTATION:
[ ] Define what framework features the gym will test
[ ] Plan required stations and their FName tags
[ ] Plan station titles and descriptions
[ ] Define console commands for testing

DURING IMPLEMENTATION:
[ ] Create GameMode inheriting from your PROJECT's base game mode:
        CkTests:      ACkTests_Gym_Base_GameMode
        Your project: your own subclass of ACk_Gym_Base_GameMode (see pattern above)
[ ] Create PlayerController inheriting from ACk_Gym_Base_PlayerController
[ ] Override Get_RequiredStations() to define all stations with titles/descriptions
[ ] Implement Request_StartGym() with gym-specific startup logic
[ ] Add console commands for individual feature testing
[ ] Use Get_StationTransform() utility for station positioning
[ ] Add proper error handling and logging
[ ] Register the gym in your project's registry namespace:
        CkTests:      CkTests_GymRegistry.as    → CkGym_Cycler::RegisterProjectGym(...)
        Your project: MyProject_GymRegistry.as   → CkGym_Cycler::RegisterProjectGym(...)
    (Keep entries sorted alphabetically within the namespace.)

AFTER IMPLEMENTATION:
[ ] Test stations spawn correctly in default grid layout
[ ] Verify station titles and descriptions display properly
[ ] Verify all console commands function
[ ] Test manual station override (place BP_DemoDisplay with matching tag)
[ ] Test in both single-player and multiplayer
[ ] Verify client-server compatibility (no GameMode dependencies in logic)
*/

//============================================================================
// 14. QUICK REFERENCE FOR COMMON TASKS
//============================================================================

//------------------------------------------------------------------------
// FINDING STATIONS
//------------------------------------------------------------------------

// Get station actor (with null check handling)
auto StationActor = Get_StationByTag("Gym.Category.Feature");

// Get station transform (with automatic fallback to FTransform::Identity)
auto StationTransform = Get_StationTransform("Gym.Category.Feature");

//------------------------------------------------------------------------
// CONSOLE COMMAND PATTERN
//------------------------------------------------------------------------

UFUNCTION(Exec, DisplayName="[Gym Name] - [Action Description]")
void Ck_Gym[GymName]_[Action]()
{
    Request_[Action]();
}

//============================================================================
// 15. ARCHITECTURE BENEFITS
//============================================================================

/*
CLIENT-SERVER SAFE:
- All gym logic is in PlayerController (exists on both client/server)
- No dependencies on GameMode availability timing
- Works consistently across network scenarios

SELF-CONTAINED:
- Each PlayerController contains all metadata and logic for its gym
- No external dependencies for basic functionality
- Easy to understand and modify

STANDARDIZED EXPERIENCE:
- Consistent console commands across all gyms
- Automatic station validation
- Unified logging and error handling

EXTENSIBLE:
- Easy to add new features to existing gyms
- Common patterns documented and reusable
- Base classes handle all boilerplate functionality
*/

//============================================================================
// 16. STATION TEXT MODIFICATION (UPDATED)
//============================================================================

/*
NEW FEATURE: Gyms can now modify station display text directly through dynamic fragments.
This allows real-time updates to station displays without needing preset text.

USAGE PATTERN:
*/

// Access the station owner (typically through entity lifetime)
auto Owner = utils_entity_lifetime::Get_LifetimeOwner(SelfEntity);

// Get or add the dynamic fragment for station display
auto& Fragment = UCk_Utils_DynamicFragment_UE::AddOrGet_Fragment(Owner, FCkGym_Station_TitleAndDescription);

// Update the title and description
Fragment.Title = FText::FromString("STATION TITLE");
Fragment.Description = FText::FromString("Dynamic description with real-time data");

/*
This allows gyms to:
- Display real-time statistics and counters
- Show attribute values and progress bars
- Update command lists dynamically
- Display network role information
- Show test results and status updates

Note: The FCkGym_Station_TitleAndDescription fragment is automatically handled by
the station display system and updates are reflected immediately on the BP_DemoDisplay.

LINE WRAPPING: The BP_DemoDisplay widget does not auto-wrap text. Any line longer
than roughly 55-60 characters will trail off the right edge of the station and
become unreadable. Use explicit '\n' line breaks in your description strings to
keep each line within that budget. This applies to AutoConfig.Description, any
f-string interpolation going into Fragment.Description, and any other text you
push to the display. Example:

    AutoConfig.Description =
        "First line, under ~55 chars.\n" +
        "Second line continues the thought.\n" +
        "Third line wraps up.";

If you find yourself needing many lines, consider trimming wording instead —
most existing gyms use 1-3 short lines total.
*/

//============================================================================
// 17. EXAMPLE: SIMPLE AUDIO GYM IMPLEMENTATION
//============================================================================

// GameMode (minimal — inherits from CkTests project base):
class ACk_AudioGym_Simple_GameMode : ACkTests_Gym_Base_GameMode
{
    default PlayerControllerClass = ACk_AudioGym_Simple_PlayerController;
    default DefaultPawnClass = ACk_Gym_Base_Pawn;
}

// PlayerController (contains all logic):
class ACk_AudioGym_Simple_PlayerController : ACk_Gym_Base_PlayerController
{
    TArray<FCkGym_Station_SpawnParams_Payload> Get_RequiredStations() override
    {
        auto Stations = TArray<FCkGym_Station_SpawnParams_Payload>();

        {
            auto Station = FCkGym_Station_SpawnParams_Payload();
            Station.Tags.Add(n"Gym.Audio.BackgroundMusic");
            Station.Title = FText::FromString("BACKGROUND MUSIC");
            auto Description = TArray<FText>();
            Description.Add(FText::FromString("Continuous background music playback."));
            Description.Add(FText::FromString("Tests audio cue execution and looping."));
            Station.Description = Description;
            Stations.Add(Station);
        }

        {
            auto Station = FCkGym_Station_SpawnParams_Payload();
            Station.Tags.Add(n"Gym.Audio.SpatialAudio");
            Station.Title = FText::FromString("SPATIAL AUDIO");
            auto Description = TArray<FText>();
            Description.Add(FText::FromString("3D positional audio effects."));
            Station.Description = Description;
            Stations.Add(Station);
        }

        return Stations;
    }

    void Request_StartGym() override
    {
        Request_StartBackgroundMusic();
        Request_StartSpatialAudio();
        ck::Trace("🎵 Simple Audio Gym - All audio features started");
    }

    void Request_StartBackgroundMusic()
    {
        auto StationTransform = Get_StationTransform("Gym.Audio.BackgroundMusic");
        utils_cue_executor::Request_ExecuteCue_Local(ck::ToEntity(this),
            utils_gameplay_tag::ResolveGameplayTag(n"AudioGym.Simple.BackgroundMusic"),
            FAudioCueTransform(StationTransform));
        ck::Trace("🎵 Background music cue executed at demo display location");
    }

    void Request_StartSpatialAudio()
    {
        auto StationTransform = Get_StationTransform("Gym.Audio.SpatialAudio");
        utils_cue_executor::Request_ExecuteCue_Local(ck::ToEntity(this),
            utils_gameplay_tag::ResolveGameplayTag(n"AudioGym.Simple.SpatialAudio"),
            FAudioCueTransform(StationTransform));
        ck::Trace("🔊 Spatial audio cue executed at demo display location");
    }

    UFUNCTION(Exec, DisplayName="Simple AudioGym - Restart Background Music")
    void Ck_GymAudioSimple_RestartBackgroundMusic() { Request_StartBackgroundMusic(); }

    UFUNCTION(Exec, DisplayName="Simple AudioGym - Trigger Spatial Audio")
    void Ck_GymAudioSimple_TriggerSpatialAudio() { Request_StartSpatialAudio(); }
}

//============================================================================
// 12. AUTO MODE PATTERN
//============================================================================

// GOAL: A gym should be immediately useful without any user input.
//
// When someone walks into a gym, they should:
//   1. See all stations actively running through their operations
//   2. See an [AUTO] indicator at the top of each station confirming it's live
//   3. See a step-by-step list of what the station is doing, with >> marking
//      the CURRENT operation — so the user understands the sequence and can
//      anticipate what comes next
//   4. See a description of what the station tests (persists after startup)
//   5. See a list of console commands they can type to interact manually
//   6. Know that typing any manual command will take them off auto mode for
//      that station, so they can explore at their own pace
//
// Not every station needs auto mode, but it's the default expectation.
// The biggest value is showing what the station is doing and what comes
// next, so the user builds a mental model of the feature without reading
// any code.
//
// DISPLAY LAYOUT (rendered by gym_auto:: helpers):
//
//   [AUTO] Running                        <- status (or [MANUAL])
//   Description of what this station      <- from AutoConfig.Description
//   tests, 1-2 lines.
//
//   Items: 5 (no limit)                   <- station-specific live data
//   [0] Potion [3/10]                        (counts, contents, grid, etc.)
//   ...
//
//   ===== Auto Sequence =====             <- plain english step descriptions
//      Add 3 potions                         >> marks the current step
//   >> Add 5 arrows                          user sees what's happening now
//      Sort inventory                        and what's coming next
//      Remove first (x3)
//
//   ===== Commands =====                  <- full console command names
//   Ck_GymMyFeature_AddPotion [n]            for manual interaction
//   Ck_GymMyFeature_SortAll
//   Ck_GymMyFeature_RestartAll
//
//   Ck_GymMyFeature_Auto [0/1]             <- global auto on/off
//   Ck_GymMyFeature_AutoUnbounded          <- per-station auto command
//
// IMPLEMENTATION:
//
// Use the shared gym_auto module (CkGym_AutoStation.as) for auto-mode
// infrastructure. It provides timer setup, message handling, display
// formatting, and stop-on-manual-command helpers.
//
// SETUP (in DoConstruct, one line):
//     AutoTimer = gym_auto::Setup(InHandle, this);
//
//     This creates a 500ms ResetOnDone timer (starts Running), binds it to
//     the "AutoTick" UFUNCTION on the entity script, and binds the
//     FCk_Message_Gym_AutoSet message to the "OnAutoSet" UFUNCTION.
//
// CONFIGURATION (declarative, in DoConstruct after Setup):
//     AutoConfig.TotalSteps = 7;
//     AutoConfig.Description = "What this station tests.";  // Use \n for line breaks (~55 chars/line — see section 16).
//     AutoConfig.GlobalAutoCommand = "Ck_GymMyFeature_Auto [0/1]";
//     AutoConfig.PerStationAutoCommand = "Ck_GymMyFeature_AutoUnbounded";
//     AutoConfig.Steps.Add(FCkGym_AutoStep("Add 3 potions", 0, 0));
//     AutoConfig.Steps.Add(FCkGym_AutoStep("Sort inventory", 3, 3));
//     AutoConfig.Steps.Add(FCkGym_AutoStep("Remove first (x3)", 4, 6));
//     AutoConfig.ManualCommands.Add("Ck_GymMyFeature_AddPotion [n]");
//     AutoConfig.ManualCommands.Add("Ck_GymMyFeature_SortAll");
//
// DISPLAY (in DisplayStats):
//     auto DisplayText = gym_auto::FormatHeader(AutoConfig, AutoRunning);
//     // ... station-specific live data ...
//     DisplayText = DisplayText + gym_auto::FormatAutoAndCommands(AutoConfig, AutoStep, AutoRunning);
//
//     FormatHeader outputs: [AUTO] Running + description.
//     FormatAutoAndCommands outputs: auto sequence with >> arrows + commands list.
//
// MANUAL HANDLERS (one-liner stop):
//     gym_auto::StopAuto(AutoTimer, AutoRunning);
//
// AUTO SET HANDLER (one-liner):
//     UFUNCTION()
//     private void OnAutoSet(FCk_Handle InHandle, FGameplayTag InMessageName, FInstancedStruct InPayload)
//     {
//         gym_auto::HandleAutoSet(InPayload, AutoTimer, AutoRunning);
//     }
//
// AUTO TICK (station-specific logic):
//     UFUNCTION()
//     private void AutoTick(FCk_Handle_Timer InHandle, FCk_Chrono InChrono, FCk_Time InDeltaT)
//     {
//         auto Step = AutoStep % AutoConfig.TotalSteps;
//         if (Step == 0) { /* operation */ }
//         // ... ensure adds == removes so state doesn't grow unbounded
//         AutoStep++;
//     }
//
// PLAYER CONTROLLER COMMANDS:
//     Ck_Gym[Name]_Auto [0/1]         — forces ALL stations on or off
//     Ck_Gym[Name]_Auto[Station]      — re-enables a specific station
//     Use BroadcastAutoToAll/BroadcastAutoToTag helpers (see inventory gym PC).
//
// KEY DESIGN DECISIONS:
// - Use explicit on/off (not toggle) to avoid mixed state across stations
// - Auto starts ON by default so the gym is alive on entry
// - Manual commands auto-disable that station (user took over)
// - Step count must balance adds and removes — count carefully or items accumulate
// - Each auto step should perform the same operation a user could do manually
//
// MANUAL COMMAND DESIGN:
// Every station should have manual console commands that let the user
// interact directly with the feature being tested. The principle:
//
//   If the auto cycle does it, the user should be able to do it too.
//
// This means:
// 1. Each distinct operation in the auto cycle maps to a console command.
//    If auto does "add modifier" and "clear modifiers", expose both as
//    Ck_GymMyFeature_AddModifier and Ck_GymMyFeature_ClearModifiers.
//
// 2. Parameterized commands are valuable. If a station manipulates a
//    numeric value, accept it as a parameter with a sensible default:
//      UFUNCTION(Exec) void Ck_GymMyFeature_SetValue(float InValue = 100.0f)
//    This lets users experiment beyond what the auto cycle shows.
//
// 3. Every manual handler MUST call gym_auto::StopAuto() first:
//      gym_auto::StopAuto(AutoTimer, AutoRunning);
//    This switches the station to [MANUAL] so the user's action isn't
//    immediately overwritten by the next auto tick.
//
// 4. Commands are listed on the station display via AutoConfig.ManualCommands.
//    Include the parameter hint in the string:
//      AutoConfig.ManualCommands.Add("Ck_GymMyFeature_SetValue [val]");
//
// 5. Use dedicated USTRUCT messages for each command. Define them in the
//    gym's Common.as file. Parameterized commands carry data in the struct:
//      USTRUCT() struct FCk_Message_MyGym_SetValue { float32 Value; ... };
//    Parameterless commands use empty structs.
//
// 6. Entity scripts bind messages in DoBeginPlay and define handler UFUNCTIONs.
//    PlayerController defines UFUNCTION(Exec) commands that broadcast the
//    message to the appropriate entity tag.
//
// EXAMPLE — per-station command flow:
//
//   // Common.as — message definition
//   USTRUCT() struct FCk_Message_MyGym_SetValue { float32 Value; };
//
//   // Entity script — bind in DoBeginPlay
//   utils_messaging::BindTo_OnBroadcast(InHandle, FCk_Message_MyGym_SetValue,
//       FCk_Delegate_Messaging_OnBroadcast(this, n"OnSetValue"));
//
//   // Entity script — handler
//   UFUNCTION()
//   private void OnSetValue(FCk_Handle InHandle, FGameplayTag InMessageName, FInstancedStruct InPayload)
//   {
//       gym_auto::StopAuto(AutoTimer, AutoRunning);
//       auto Typed = InPayload.Get(FCk_Message_MyGym_SetValue);
//       // ... apply Typed.Value ...
//   }
//
//   // PlayerController — console command
//   UFUNCTION(Exec, DisplayName="My Gym - Set Value")
//   void Ck_GymMyFeature_SetValue(float InValue = 100.0f)
//   {
//       for (auto E : utils_entity_tag::ForEach_Entity(ck::ToEntity(this), n"TAG_MyStation"))
//       { utils_messaging::Broadcast(E, FCk_Message_MyGym_SetValue(InValue)); }
//   }
//
// STATION DESCRIPTION PERSISTENCE:
// The station's initial Description (set in Get_RequiredStations) is overwritten
// on the first DisplayStats tick. To keep it visible, set AutoConfig.Description
// which gym_auto::FormatHeader renders after the status line. Match the text in
// Get_RequiredStations so both the brief flash and the live display agree.
//
// IMPORTANT: Don't nest function calls with string args inside f-strings.
// f"{func("arg")}" breaks — the inner quotes terminate the f-string.
// Instead: DisplayText = DisplayText + func("arg");
//
// See CkGym_AutoStation.as for the full implementation.
// See CkInventoryGym_DataOnly_Unbounded.as for a reference station.

//============================================================================
// 13. INLINE ASSET DEFINITIONS
//============================================================================

// Use the Angelscript 'asset' keyword to define test data assets inline
// instead of creating .uasset files in the content browser.
//
// Files containing asset blocks must use the _Assets.as suffix:
//   GOOD: CkInventoryGym_Assets.as
//   BAD:  CkInventoryGym_Shared.as (assets mixed with logic)
//
// PATTERN:
//   asset ItemDef_MyGym_Sword of UCk_InventoryItem_Definition
//   {
//       _CoreInfo = FCk_InventoryItem_CoreInfo(FText::FromString("Sword"));
//       auto Trait = Cast<UCk_ItemTrait_Tags>(NewObject(this, UCk_ItemTrait_Tags));
//       Trait._Tags = ...;
//       _ItemTraits.Add(Trait);
//   }
//
// ACCESSOR NAMESPACE:
//   namespace my_gym_items
//   {
//       UCk_InventoryItem_Definition Sword() { return ItemDef_MyGym_Sword; }
//   }
//
// The asset keyword creates a real UObject at module load — no .uasset on disk,
// discoverable by the asset registry, hot-reloadable. See CkInventoryGym_Assets.as
// for a complete example with stackable/tags/dimensions traits.

//============================================================================
// 14. REPLICATION GOTCHAS
//============================================================================

// The gym framework's PlayerController and station actors have Replicates=false.
// The Pawn has Replicates=true. This matters for any feature that routes through
// Request_BuildAndReplicate (e.g. item creation):
//
// - Item creation via Request_AddItemByDefinition requires the inventory's
//   ContextOwner to have replicated NetParams. In standalone this was fixed
//   by making Request_TryBuildAndReplicate build locally when the owner
//   doesn't replicate (see CkEntityReplicationDriver_Utils.cpp fix).
//
// - If you hit silent failures (e.g. Failed_NoSpaceAvailable on unbounded
//   inventories), check the replication chain: ContextOwner → NetParams →
//   Get_EntityReplication. The error name is misleading.
//
// - For multi-client PIE testing, ensure the entity creation path has a
//   replicated owner in the chain.

//============================================================================
// 15. LEVEL SETUP & BINARY PIPELINE (THINGS CLAUDE CAN'T SEE)
//============================================================================

// Several critical pieces of the gym framework are binary assets in the editor
// that AngelScript code depends on but cannot directly inspect:
//
// LEVEL REQUIREMENTS — Each gym level (.umap) must have:
//   1. World Settings → Default GameMode Override set to the project's base
//      game mode (e.g. ACkTests_Gym_Base_GameMode for CkTests, or your own
//      project base). Without this, no gyms register and the Tab menu is empty.
//   2. A Blueprint actor in the level that hooks onto the transient entity
//      and listens for messages that trigger BP_DemoDisplay spawning.
//      This is the bridge between the AS CkGym_Common::Request_SpawnNewStation()
//      call and the actual display hardware appearing in the world.
//
// BP_DemoDisplay PIPELINE — The display stations visible in-game are Blueprint
// actors (BP_DemoDisplay) with this internal flow:
//   1. BP_DemoDisplay spawns → it adds an entity script onto itself
//   2. That entity receives FCkGym_Station_TitleAndDescription dynamic fragments
//      (sent by the gym station scripts via Owner.AddOrGet_Fragment)
//   3. The fragment data (Title, Description) is rendered onto 3D text
//      components on the display actor each tick
//
// This means:
//   - Creating a new gym level: duplicate an existing gym level
//     (TestGyms_CkTests_Level or TestGyms_BB_MAP) so you get the
//     spawn-listener Blueprint for free.
//     Then change the World Settings game mode override.
//   - BP_DemoDisplay is a Content asset in Plugins/CkTests/Content/Common/DemoRoom/.
//     Both CkTests and BB levels reference it — it's part of the framework.
//   - If station text isn't updating, check that the entity script is writing to
//     the LIFETIME OWNER's fragment (utils_entity_lifetime::Get_LifetimeOwner),
//     not the station entity directly.
//
// HUD — ACkGym_MenuHUD (set by ACk_Gym_Base_GameMode via default HUDClass):
//   - Renders the Tab-key gym selector overlay
//   - Reads gym entries from CkGym_Cycler::Get_GymRegistry() (= Subsystem.ProjectGyms)
//   - Only shows gyms that were registered by the current level's game mode
//   - Info about which gyms exist comes through UCkGym_CyclerSubsystem

/*
This specification provides everything needed to quickly create consistent,
robust gyms following established patterns. The framework is project-agnostic;
each project registers its own gyms from its own base game mode.
*/